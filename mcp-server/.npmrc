# Project-local npm config. Pinned so that a contributor whose
# personal ~/.npmrc points at a private mirror (e.g. a corporate
# proxy) does NOT leak non-public resolved URLs into `package-lock.json`
# when they run `npm install` — which previously broke CI on a
# public GitHub runner with E403 on every tarball fetch.
registry=https://registry.npmjs.org/
